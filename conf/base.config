process {
  cpus = 1
  memory = 1.GB
  time = 5.m
}

params {
  max_cpus = 2 // Base specifications
  max_memory = 7.GB // Base specifications
  max_time = 1.h // Base specifications
  outDir = "${PWD}" // Path to output directory
}
